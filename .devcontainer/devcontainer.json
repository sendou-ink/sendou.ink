{
	"name": "sendou.ink",
	"build": {
		"dockerfile": "Dockerfile"
	},
	"features": {
		"ghcr.io/devcontainers/features/node:1": {
			"version": "lts/jod"
		}
	},
	"forwardPorts": [5173],
	"postCreateCommand": "sudo chown -R vscode:vscode /workspaces/sendou.ink && npx playwright install --with-deps && npm install"
}
